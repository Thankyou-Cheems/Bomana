[project]
name = "bomana"
version = "6.9.0"
description = "War Thunder SB timer and navigation assistant"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.25.0",
    "certifi>=2021.0.0",
    "pillow>=8.0.0",
    "pystray>=0.17.0",
]

[project.optional-dependencies]
build = [
    "pyinstaller>=5.13.0",
]

[tool.uv]
package = false
